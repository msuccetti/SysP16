package robot.cases;

public class StampCatchCase {
	/*starte_hub_motor_senken();
	 * if(encoder_finish=true)
	 * {
	 *   hub_motor_stop();
	 *   drive_back();
	 * };
	 * if(encoder_finish=true)
	 * {
	 * 	 drive_back_stop();
	 * 	 start_hub_motor();
	 * };
	 * if(encoder_finish=true)
	 * {
	 * 	 hub_motor_stop();
	 * };
		
**/
}
